import * as React from 'react';

export default () => {
  return (
    <div style={{display: 'block', fontSize: '12px', margin: '0'}}>
      <span style={{border: '0 px', whiteSpace: 'nowrap'}}>test</span>
    </div>
  );
}
